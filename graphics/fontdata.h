#pragma once
#include <circle/types.h>

namespace hfh3
{
    extern const u8 font_data[];
    extern const int font_offset;   // first char is the space character
    extern const int font_width;    // there are 16*6 bytes per line in the font
    extern const int font_height;   // each character is 8 pixels high;
}
